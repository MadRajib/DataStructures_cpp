#include <string>
#include <vector>
#include "lcgs.h"

void randomise(std::vector<int> &arr);

void pickRandom(const std::vector<int> &arr,std::vector<int> &pick,const int );

void pickMFromN(std::vector<int> ar1,std::vector<int> &pick,const int );